package main //This declares the package as "main", which defines a standalone executable. When running a golang program, "main" package is entry point

import "fmt" //fmt package "implements formatted I/O with functions analogous to C's printf and scanf. The format 'verbs' are derived from C's but are simpler"
		// https://pkg.go.dev/fmt

func main() {
	fmt.Println("Hello, World!")
}
